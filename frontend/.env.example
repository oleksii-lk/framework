# Copy this file to .env.local and adjust for your environment.
# Vite exposes variables prefixed with VITE_ to the browser bundle.
# .env.local is gitignored by default; .env.example is committed as documentation.

VITE_API_URL=http://localhost:8000
